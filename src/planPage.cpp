#include <gtkmm.h>
#include "app.hpp"

Gtk::ScrolledWindow* planList(const Glib::ustring& planName) {
    // todo header on top showing back button, centered text with plan name and create button on right and scrollable in center
}
